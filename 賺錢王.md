---
name: 賺錢王
description: |
  香港商家 AI 客服機會掃描器。自動掃描香港商家，找出 2 類潛在客戶：
  A. 沒網頁的商家 → 可推銷「建網站 + AI 客服」方案
  B. 有網頁但沒 AI 客服的商家 → 可推銷「加裝 AI 客服」方案
  每個商家自動提取 WhatsApp / Email 聯絡方式（不取電話、不取地址），供業務人員直接開發客戶。
  當用戶說「/skill 賺錢王」「跑掃描」「掃描商家」「找客戶」「香港商家機會」時觸發。
  執行流程：saferun.sh（掃描）→ build_mokcs.py（克隆商家頁面到 MokCs 注入 AI 客服 Demo）。
---

# 🤖 賺錢王 · 香港商家 AI 客服機會掃描器

## 這是什麼？
自動掃描香港商家，找出 2 類潛在客戶並提取 WhatsApp / Email 聯絡方式（不取電話、不取地址），供業務人員開發。

## 執行方式
說「/skill 賺錢王」時，自動執行：
1. `bash saferun.sh` — Google Maps 掃描 + AI客服檢測 + 提取 WhatsApp/Email + 去重 + 報告
2. `python3 build_mokcs.py` — 克隆有 WhatsApp/Email 的商家首頁到 MokCs，注入 AI 客服 Agent 做 Demo

## 檔案位置
- 技能目錄：`~/.mok/skill/賺錢王/`
- 主程式：`scanner.py`、`build_mokcs.py`
- 執行腳本：`run.sh`（手動互動）、`saferun.sh`（重新掃描）、`auto_run.sh`（自動，cron 每 30 分鐘，僅香港時間 08:00-22:00）
- 報告：`報告.html`
- 去重記錄：`leads_7days.jsonl`（7 天自動清除舊資料）
- 日誌：`scanner.log`、`mokcs_build.log`、`auto_run.log`

## 手動執行
```bash
cd ~/.mok/skill/賺錢王 && ./run.sh          # 互動模式（可自訂行業）
python3 scanner.py 餐廳                     # 只掃指定行業
python3 scanner.py 餐廳 --limit 30          # 限制數量
python3 build_mokcs.py                      # 掃完後克隆有 WhatsApp/Email 的商家頁面
```

## 聯絡方式規則（重要）
- 報告只顯示 WhatsApp 與 Email，不要電話、不要地址
- 從商家網站 HTML 自動提取：
  - WhatsApp：`wa.me/號碼`、`api.whatsapp.com/send?phone=`、`whatsapp.com/channel`
  - Email：`mailto:`、頁面中的 email 地址
- 7 天內已記錄的商家自動跳過（leads_7days.jsonl 去重）

## 自動化（cron）
- `*/30 * * * * /bin/bash ~/.mok/skill/賺錢王/auto_run.sh`
- auto_run.sh 內部判斷香港時間 08:00-22:00 才執行，避免夜間打擾
