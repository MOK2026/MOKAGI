# 補丁：只掃單層（2026-08-30 15:55）

## 做什麼
讓 `/api/tree` 檔案樹**只掃一層**（不遞迴），徹底解決遞迴掃描 home 造成
2MB JSON / 1.5s+ 延遲 / 524 超時的問題。檔案樹秒開。

## 怎麼改
- 改 `保丁.py` 裡的 `MAX_TREE_DEPTH`：
  - `1` = 只顯示一層（目前）
  - `2` = 顯示兩層
  - `0` = 完全恢復原版無限遞迴
- 改完**重啟 mok_web** 即生效，核心檔 mok_web.py 完全不用動。

## 停用
把目錄改名，前面加底線：`_只掃單層_2026_08301555`，重啟即恢復原版行為。
